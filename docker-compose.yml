services:
  mongodb:
    image: mongo:7
    container_name: flask_mongo_crud_db
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:

